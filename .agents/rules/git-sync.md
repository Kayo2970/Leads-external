# Git Auto-Sync Rule

Always stage, commit with a concise descriptive message, and push all changes to the remote repository (`git add -A; git commit -m "..."; git push origin main`) immediately after making changes and verifying them with `npm run build`.
